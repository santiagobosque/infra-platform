How to run?

Create terraform.tfvars which contains variables:

virtual_environment_endpoint

virtual_environment_api_token

virtual_environment_node_name

ubuntu_template_id

ip_address

gateway

datastore_id

then run:

terraform init
terraform plan
terraform apply